learning git bash first day
git config --global user.name "git hub username"
git config --global user.email "git mail"
git remote add origin "link taken from githhub"
ls = is used for listing all the file name .
nano = is used to create a file or can use a created file .
git add  "use the name of the file " = is used to change from untracked files to tracked file .
git commit -m "can write anything here " = is used for commiting stored chnages .
git push = is used to push the file into the  git hub .
git pull = is used to pull the changes made in the from git hub . 
git add . = is used to change all files from untracked to tracked 
 

i have created a branch master2 
